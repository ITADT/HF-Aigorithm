# 部署说明 —— 在其他计算机上需要更改的地方

> 本文档面向：把本目录代码拿到另一台机器（装有红枫 2026 仿真平台）上运行的人。
> 核心原则：**所有本机专属路径都集中在少数几个位置**，改完即可运行。

## 1. 环境要求

- 红枫 2026 仿真平台包（含 `opensim-sim.exe`、`competition`、`dataset`、`python`）；
- 推荐使用**仿真包内捆绑的 Python**（`<仿真包>/python/python.exe`，自带
  ultralytics/torch/redis 等依赖）；
- 若用系统 Python，需自行安装依赖（见第 5 节）。

## 2. 必须改的路径（按优先级）

### 2.1 仿真包根目录（必须）
将代码放到仿真平台`hf2026-sim-windows\competition\user_algorithms\coop_decoy`
目录下

### 2.2 YOLO 模型权重路径（建议改）

文件：`yolo_proc.py`（第 40~41 行附近）

```python
DEFAULT_MODEL_PATH = os.environ.get(
    "YOLO_MODEL",
    r"E:\osreadm\hf2026-sim-windows\examples\yolotrack\target_vehicle_yolov8s.pt")
```

- 官方权重通常在仿真包的 `examples/yolotrack/target_vehicle_yolov8s.pt`；
- 若路径不同，改默认值，或设环境变量 `YOLO_MODEL`（推荐）：

```powershell
$env:YOLO_MODEL = "D:\opensim2026\examples\yolotrack\target_vehicle_yolov8s.pt"
```

### 2.3 ultralytics 配置目录（权限报错时改）

文件：`yolo_proc.py`（第 30 行附近）

```python
_DEFAULT_CONFIG_DIR = r"D:\HF\.ultralytics"
```

- 作用：ultralytics 需要可写配置目录，默认 `%APPDATA%\Ultralytics` 常被权限挡，
  导致 eval 崩溃；
- 运行脚本已自动设 `YOLO_CONFIG_DIR = <HF>\.ultralytics`，一般无需改；
- 若你的用户目录不可写，设 `YOLO_CONFIG_DIR` 指向任意可写目录。


## 3. 平台（可视化仿真）使用步骤

1. 复制 `main` + `yolo_proc.py` 到
   `<仿真包>/competition/user_algorithms/coop_decoy/`；
   - **文件名必须是合法 Python 模块名**（字母/数字/下划线，不能含 `.`、`-`），
     例如 `main` 可以，`v1_coop_simple_v4.2y.py` 不行（点号会导致
     competition_crashed）；
2. 前端右下角「赛题与算法」→ coop_decoy → 刷新 → 选中算法 → 开始仿真；
3. 默认前端启动不启用 YOLO（`photoMode:auto`）。要启用 YOLO，通过
   `http://localhost:8081/api/sim/start` POST JSON：

```json
{
  "scenario": "coop_decoy",
  "agent": "user_algorithms.coop_decoy.v1_coop_simple:V1CoopAgent",
  "mode": "eval",
  "yoloModel": "examples/yolotrack/target_vehicle_yolov8s.pt",
  "photoMode": "auto"
}
```

4. 无 UE 渲染画面（headless）时 `obs.self.photo` 恒 None，YOLO 不生效，
   自动回退 AccuracySimulator——这是正常降级，不是 bug。

## 4. 导入方式（已兼容，无需改）

`main` 顶部对 `yolo_proc` 的导入已做双通道兼容：

```python
try:
    from . import yolo_proc      # 平台：与 yolo_proc.py 同目录/同包
except Exception:
    try:
        from Code import yolo_proc   # 本地开发：Code 包
    except Exception:
        yolo_proc = None
```

- 平台下两个文件放同一目录即可，YOLO 自动启用；
- 两个文件都不在时 `yolo_proc=None`，算法仍可运行（YOLO 降级关闭）。

## 5. Python 与依赖

- 可视化平台 bridge 默认用系统 Python（`C:\Python314`/`C:\Python313`/
  `C:\Python312` 或 PATH 里的 `python`）——若系统 Python 缺少依赖，YOLO 会
  加载失败并降级（不崩溃）。让平台用包内 Python：启动平台前设

```powershell
$env:PYTHON_BIN = "E:\osreadm\hf2026-sim-windows\python\python.exe"
```

- 若坚持用系统 Python，需要：`pip install ultralytics opencv-python redis
  numpy torch torchvision`（版本与包内一致即可）。
- 或者直接在仿真平台目录：hf2026-sim-windows\python下执行(最简单)：
```powershell
python.exe -m pip install -r requirements.txt
```

